<main>
	<section class="createHeader">
 
		<h2 class="liftTitle">{{title}}</h2>
		<button type="button" id="addLift">Add Lift</button>

	</section>

	<section class="createContainer">

		<article class="lift">


			<table class="liftTable">
				<tr>
					<th>Muscle</th>
					<th>Exercise Name</th>
					<th>Sets</th>
					<th>Reps</th>
					<th>Rest</th>
					<th></th>
				</tr>
				{{#each lifts}}
				<tr>
					<td>{{muscle}}</td>
					<td>{{liftName}}</td>
					<td>{{sets}}</td>
					<td>{{reps}}</td>
					<td>{{rest}} {{unit}}</td>
					<td><input type="button" value="Delete" onclick="deleteRow(this)"/></td>
				</tr>
				{{/each}}
			</table>

		</article>

	
	</section>
	<div class="saveButton" id="save-button"><button type="button">Save Plan</button></div>

</main>